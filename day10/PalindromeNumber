class Solution(object):
    def isPalindrome(self, x):
        org=x
        rev=0
        while x>0:
            rem=x%10
            rev=rev*10+rem
            x/=10
        if org==rev:
            return True
        else:
            return False
